# ClaudeDeck Miku server — fairseq-free RVC. No fairseq, no special Python needed.
# PyTorch is installed separately by run.bat (CUDA build from the pytorch index).
fastapi
uvicorn
edge-tts
pydub
numpy
scipy
soundfile
librosa
faiss-cpu>=1.8
# transformers 5.x needs torch>=2.7 (torch.float8_e8m0fnu); pin to the well-tested
# 4.x line that ContentVec / content-vec-best run on.
transformers>=4.40,<5
huggingface_hub
